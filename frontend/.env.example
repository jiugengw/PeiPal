# Optional in development because Vite proxies /api to FastAPI on port 8000.
VITE_API_BASE_URL=
VITE_MCP_URL=http://localhost:8001/mcp

# Public browser credentials from Supabase Project Settings > API.
# Never use the service-role key in the frontend.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
